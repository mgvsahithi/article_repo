/*package api.mapper;

import org.mapstruct.Mapper;

import api.bo.Article;
import api.bo.User;
import api.request.UserRegistrationRequest;

@Mapper(componentModel = "spring", uses= {Article.class})
public abstract class UserMapper {

	public abstract User userRegisterationRequestToUser(UserRegistrationRequest userRegisterationRequest);
}
*/